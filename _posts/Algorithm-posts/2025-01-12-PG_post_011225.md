---
title: "[프로그래머스] Lv.3 네트워크 - Python"
date: 2025-01-12  
categories: [코딩테스트 연습, 프로그래머스]
tags: [Python, Algorithm]
layout: post
author: mongblang
---

# 📌&nbsp; **네트워크**
[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/43162)  

# 📝&nbsp; **문제**
---
![문제](/assets/img/codingtest-post-img/PG43162-1.png) 

## ✏️&nbsp; **입출력 예시**
---
![예시](/assets/img/codingtest-post-img/PG43162-2.png) 
![예시1그림](https://grepp-programmers.s3.amazonaws.com/files/ybm/5b61d6ca97/cc1e7816-b6d7-4649-98e0-e95ea2007fd7.png)
![예시2](/assets/img/codingtest-post-img/PG43162-3.png) 
![예시2그림](https://grepp-programmers.s3.amazonaws.com/files/ybm/7554746da2/edb61632-59f4-4799-9154-de9ca98c9e55.png)

&nbsp;  

&nbsp;   



## **내 풀이**  
--- 

```python
def dfs(n,computers, visited, current):
    visited[current] = True
    for idx in range(n):
        if computers[current][idx] == 1 and visited[idx] == False:
            dfs(n, computers, visited, idx) 
            
def solution(n, computers):
    visited = [False] * n
    networks = 0
    
    for i in range(n):
        if visited[i] == False:
            dfs(n,computers,visited,i)
            networks += 1
    return networks
```


#### **코드 해설**  
1. 연결정보를 확인하고 방문상태를 확인/갱신할 dfs함수를 정의 - 현재 노드를 True로 방문처리하고, 컴퓨터의 개수만큼 순회하며 현재 노드와 연결된 노드가 있고, 방문하지 않았을 때
연결된 노드에 대해 재귀호출한다.
2. 메인 함수에서 방문리스트를 False로 초기화하고 네트워크 개수를 기록할 변수도 0으로 초기화한다.
3. 컴퓨터의 개수만큼 순회하며 방문하지 않은 노드인 경우 재귀호출해 탐색한다. 
4. 탐색이 끝나면 더이상 연결된 노드가 없으니 네트워크 개수를 1씩 증가시킨 뒤 최종 개수를 반환한다. 

&nbsp;  

## **다른 풀이**
---

```python  
def solution(n, computers):
    answer = 0
    visited = [0 for i in range(n)]
    def dfs(computers, visited, start):
        stack = [start]
        while stack:
            j = stack.pop()
            if visited[j] == 0:
                visited[j] = 1
            # for i in range(len(computers)-1, -1, -1):
            for i in range(0, len(computers)):
                if computers[j][i] ==1 and visited[i] == 0:
                    stack.append(i)
    i=0
    while 0 in visited:
        if visited[i] ==0:
            dfs(computers, visited, i)
            answer +=1
        i+=1
    return answer
```

#### **코드 해설**  
1. 네트워크 개수를 저장할 변수 `answer`과 방문여부를 저장할 `visited`를 각 0으로 초기화한다. 
2. 스택을 사용해 dfs함수를 구현한다 - 스택에 원소가 있을 때 현재 노드를 꺼내서 방문여부를 갱신한다. 
3. `computers`의 수만큼 순회하며 연결된 노드가 존재하고 방문하지 않은 노드가 있는 경우 스택에 추가한다. 
4. 방문하지 않은 노드가 없을때까지 재귀호출하며 탐색한 뒤 answer를 1로 증가시킨다. 다음 노드로 넘어가기 위해 index역할을 하는 `i`도 1씩 증가시킨다. 
5. 최종 네트워크 개수를 반환한다. 



&nbsp;   
&nbsp;  
