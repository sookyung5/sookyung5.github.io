---
title: "[프로그래머스] Lv.2 모음 사전 - Python"
date: 2025-01-09  
categories: [코딩테스트 연습, 프로그래머스]
tags: [Python, Algorithm]
layout: post
author: mongblang
---

# 📌&nbsp; **모음 사전**
[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/84512)  

# 📝&nbsp; **문제**
---
![문제](/assets/img/codingtest-post-img/PG84512-1.png)

## ✏️&nbsp; **입출력 예시**
---
![예시](/assets/img/codingtest-post-img/PG84512-2.png) 

&nbsp;  

&nbsp;   



## **내 풀이**  
--- 

```python
def solution(word):
    vowels = ["A", "E", "I", "O", "U"]
    words = []
    
    def word_dict(current_word, max_length):
        if current_word:
            words.append(current_word)
        
        if len(current_word) == max_length:
            return
        
        for v in vowels:
            word_dict(current_word + v, max_length)
    
    word_dict("",5)
    return words.index(word)+1
```


#### **코드 해설**  
1. 모음이 담긴 리스트 `vowels`를 생성한다.
2. 중복순열로 생성한 모든 단어들을 저장할 빈 리스트 `words`를 생성한다.
3. 재귀함수 word_dict를 사용해 중복순열을 구현한다. 먼저, current word가 빈 문자열이 아닐 때 생성한 단어들을 words에 추가한다. 
4. 현재 생성한 단어의 길이가 최대 길이 (모음의 개수)와 같으면 종료한다. 
5. 재귀호출하며 현재 단어에 각 모음을 추가한 모든 단어들을 생성한다. 
6. 생성된 단어 리스트에서 주어진 단어의 index값에 1을 더한 값을 반환한다. 

&nbsp;  

## **다른 풀이**
---

```python  
from itertools import product

def solution(word):
    vowels = ["A", "E", "I", "O", "U"]
    words = []
    
    for i in range(1,len(vowels)+1):
	    prod = product(vowels, repeat=i)
	    
	    for p in prod:
		    words.append(''.join(p))
	
	words.sort() 
	return words.index(word)+1
```

#### **코드 해설**  
1. 작동방식은 위와 같지만 itertools의 product 함수를 사용해서 풀어보았다. 먼저 1부터 6까지 반복하면서 중복순열을 생성한다. 
2. 튜플로 생성된 단어들을 join해 words에 추가한다.
3. 사전순으로 들어가지 않으므로 words를 정렬해준 뒤 index를 사용해 주어진 단어의 순서를 반환한다. 

&nbsp;   
&nbsp;  
