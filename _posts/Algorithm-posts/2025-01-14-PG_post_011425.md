---
title: "[프로그래머스] Lv.3 아이템 줍기 - Python"
date: 2025-01-14  
categories: [코딩테스트 연습, 프로그래머스]
tags: [Python, Algorithm]
layout: post
author: mongblang
---

# 📌&nbsp; **아이템 줍기**
[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/87694)  

# 📝&nbsp; **문제**
---
문제 설명
다음과 같은 다각형 모양 지형에서 캐릭터가 아이템을 줍기 위해 이동하려 합니다.  

![rect_1](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/9b96b07f-72db-4b1c-bd7a-6a9c9b8d0dc6/rect_1.png)

지형은 각 변이 x축, y축과 평행한 직사각형이 겹쳐진 형태로 표현하며, 캐릭터는 이 다각형의 둘레(굵은 선)를 따라서 이동합니다.  

만약 직사각형을 겹친 후 다음과 같이 중앙에 빈 공간이 생기는 경우, 다각형의 가장 바깥쪽 테두리가 캐릭터의 이동 경로가 됩니다.  

![rect_2](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/38b0739b-8dd8-40d8-ac44-c71678d28d07/rect_2.png)  

단, 서로 다른 두 직사각형의 x축 좌표 또는 y축 좌표가 같은 경우는 없습니다.  
  
![rect_4](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/ec976181-987e-494e-bb2d-0615ce16252f/rect_4.png)  

즉, 위 그림처럼 서로 다른 두 직사각형이 꼭짓점에서 만나거나, 변이 겹치는 경우 등은 없습니다.  

다음 그림과 같이 지형이 2개 이상으로 분리된 경우도 없습니다.  
 
![rect_3](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/7eda8d92-ebe0-4b5f-bd15-0c9dc7af3a3e/rect_3.png)  

한 직사각형이 다른 직사각형 안에 완전히 포함되는 경우 또한 없습니다.  

![rect_7](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/1e178b0d-6580-4981-aae3-dd82a1b95362/rect_7.png)

지형을 나타내는 직사각형이 담긴 2차원 배열 rectangle, 초기 캐릭터의 위치 characterX, characterY, 아이템의 위치 itemX, itemY가 solution 함수의 매개변수로 주어질 때, 캐릭터가 아이템을 줍기 위해 이동해야 하는 가장 짧은 거리를 return 하도록 solution 함수를 완성해주세요.   

&nbsp;

**제한사항**  
- rectangle의 세로(행) 길이는 1 이상 4 이하입니다.
- rectangle의 원소는 각 직사각형의 [좌측 하단 x, 좌측 하단 y, 우측 상단 x, 우측 상단 y] 좌표 형태입니다.
    - 직사각형을 나타내는 모든 좌표값은 1 이상 50 이하인 자연수입니다.
    - 서로 다른 두 직사각형의 x축 좌표, 혹은 y축 좌표가 같은 경우는 없습니다.
    - 문제에 주어진 조건에 맞는 직사각형만 입력으로 주어집니다.
- charcterX, charcterY는 1 이상 50 이하인 자연수입니다.
    - 지형을 나타내는 다각형 테두리 위의 한 점이 주어집니다.
- itemX, itemY는 1 이상 50 이하인 자연수입니다.
    - 지형을 나타내는 다각형 테두리 위의 한 점이 주어집니다.
- 캐릭터와 아이템의 처음 위치가 같은 경우는 없습니다.  

&nbsp;

## ✏️&nbsp; **입출력 예시**
---
![예시](/assets/img/codingtest-post-img/PG87694-1.png)

&nbsp;

**입출력 예 설명** 
- 입출력 예 #1   
![rect_5](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/7b89552b-f7b6-47e7-8bbd-deaf01907f70/rect_5.png)  
캐릭터 위치는 (1, 3)이며, 아이템 위치는 (7, 8)입니다. 위 그림과 같이 굵은 선을 따라 이동하는 경로가 가장 짧습니다.  

- 입출력 예 #2  
![rect_6](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/ac6911d0-e386-472b-a109-2542214c8d6b/rect_6.png)  
캐릭터 위치는 (9, 7)이며, 아이템 위치는 (6, 1)입니다. 위 그림과 같이 굵은 선을 따라 이동하는 경로가 가장 짧습니다.  

- 입출력 예 #3  
![rect_8](https://grepp-programmers.s3.ap-northeast-2.amazonaws.com/files/production/9c47ca5c-df4b-4b2e-8c5b-faf0815de665/rect_8.png)  
캐릭터 위치는 (1, 1)이며, 아이템 위치는 (4, 7)입니다. 위 그림과 같이 굵은 선을 따라 이동하는 경로가 가장 짧습니다.  


&nbsp;  

&nbsp;   



## **내 풀이**  
--- 

```python
from collections import deque

def solution(rectangle, characterX, characterY, itemX, itemY):
    map = [[0] * 101 for _ in range(101)]

    for x_1, y_1, x_2, y_2 in rectangle:
        x1, y1, x2, y2 = x_1 * 2, y_1 * 2, x_2 * 2, y_2 * 2

        for r in range(x1, x2+1): 
            for c in range(y1, y2+1):  
                map[r][c] = 1 
    
    for x_1, y_1, x_2, y_2 in rectangle:
        x1, y1, x2, y2 = x_1 * 2, y_1 * 2, x_2 * 2, y_2 * 2

        for r in range(x1+1, x2): 
            for c in range(y1+1, y2): 
                map[r][c] = 0
                
    dx = [1,-1,0,0] 
    dy = [0,0,1,-1]  

    start_x, start_y = characterX * 2, characterY * 2
    end_x, end_y = itemX * 2, itemY * 2

    queue = deque()
    queue.append((start_x, start_y, 0))
    visited = [[0] * 101 for _ in range(101)]
    visited[start_x][start_y] = 1

    n = len(map)
    m = len(map[0])

    while queue:
        x, y, dist = queue.popleft() 

        if x == end_x and y == end_y:
            return dist // 2 

        for i in range(4):
            next_x = x + dx[i]
            next_y = y + dy[i]

            if 0 <= next_x < n and 0 <= next_y < m:
                if map[next_x][next_y] == 1 and visited[next_x][next_y] == 0:
                    visited[next_x][next_y] = 1 
                    queue.append((next_x, next_y, dist + 1))

    return None
```

#### **풀이 과정**
시작점에서 끝점까지의 **최단경로**를 구하는 문제이므로 BFS를 사용해서 풀어봤다.  
이 문제의 핵심은 "직사각형이 겹쳐서 생기는 내부 공간"을 필터링해 캐릭터가 테두리로만 이동하게 해야 한다는 것이다. BFS는 기본적으로 **격자(Grid) 상에서 정수 단위로 한 칸씩 이동**하며 최단 거리를 탐색하는데, 주어진 좌표 그대로 사용한다면 대각선으로 인접한 칸도 유효한 경로라고 판단해 최단 경로가 짧아지는 문제가 생긴다. 따라서 맵을 생성할 때 좌표 스케일링을 통해 모든 좌표를 2배로 만들어 주는 작업이 필수적이다. 

1. 좌표 스케일링 - 모든 좌표 2배
    - 맵 생성: 직사각형을 나타내는 모든 좌표값은 1 이상 50 이하인 자연수이므로, 맵의 최대 크기를 50*2 + 1으로 설정한다. 경우에 따라 102로 여유공간을 늘릴 수 있음
    - 시작, 끝 좌표 2배: 시작 좌표를 나타내는 `characterX,Y`와 끝 좌표인 `itemX,Y`를 2배로 확대한다. 

2. 테두리 찾기 - 직사각형이 겹친 내부 공간 필터링
    - 직사각형을 생성할 때, 모든 공간을 특정 값 (예:1)로 설정한 후, 테두리를 뺀 내부 공간을 다시 특정 값 (예:0)으로 채워서 테두리(=1)만 이동이 가능하도록 한다.
    - 테두리 조건: 특정 칸(x,y)는 적어도 하나의 직사각형 내부에 포함되어야 하며, (x,y)와 인접한 칸 중 적어도 하나는 내부에 포함되지 않아야 한다. 
3. BFS 탐색
    - 시작: 덱에 (x, y, 거리)를 튜플 형태로 추가
    - 방문처리: 생성한 맵과 같은 맵을 하나 더 생성한 후 0으로 채워 방문 여부를 표시한다.
    - 탐색: 탐색할 좌표를 덱에서 pop한 후 이동 가능한 칸을 탐색
        - 탐색 조건: 맵을 벗어나지 않으며, 이미 방문하지 않은 칸인 경우 탐색 
    - 종료 조건: 현재 (x,y)가 `itemX,Y와` 같은 경우 거리를 2로 나눈 값 (원래 크기로 돌려놓음)을 반환
    - 예외: 종료 조건에 걸리지 않고, 이동이 더이상 불가능한 경우 반환할 값 설정 

#### **코드 해설**  
1. 직사각형이 위치할 격자 맵을 생성한다. 위에 언급한대로 좌표를 2배로 확대해야 하므로 최대 크기를 50*2 + 1인 101로 설정한다 (인덱스가 0으로 시작하므로 +1을 해주었다).
2. rectangle의 각 x,y 좌표를 순회하며 2배로 늘려준 뒤 생성된 맵 상에 표시한다. 이때, 모든 공간을 1로 채워준다. 
3. 다시 반복문을 사용해 **테두리를 제외한 직사각형 내부 공간**, 즉 (x_1+1 ~ x_2) 와( y_1 +1 ~ y_2) 범위의 공간을 0으로 채워 이동이 불가능하도록 한다. 이렇게 하면 캐릭터가 1인 값을 (테두리) 따라 이동하기 때문에 내부로 이동하지 않는다.
4. 인접칸을 탐색할 4방향을 정의해준 뒤 시작 좌표와 목표 좌표를 2배로 늘려서 새 변수에 할당한다.
5. 맵의 가로, 세로 크기를 각 좌표에 할당한다. 사실 이 값은 상수이므로 처음에 변수 하나에 할당하면 이 작업은 필요가 없다. 
6. 큐가 빌때까지 BFS탐색을 진행한다. 먼저, 시작 좌표와 거리에 해당하는 값인 0을 튜플형태로 덱에 넣어준다. 
7. 방문여부를 표시할 맵을 새로 생성한 뒤 0으로 채워준다. 
8. 종료 조건을 설정한다. 현재 위치가 목표 위치와 같은 경우 거리를 2로 나눈 값을 반환한다. 
9. 시작점을 방문처리 하고, 인접한 4방향을 탐색하며 인접한 칸이 맵을 벗어나지 않고, 테두리에 해당되며, 방문하지 않은 곳인 경우 방문 처리를 한 뒤 해당 좌표를 덱에 새로 추가한다.
10. 목표 위치에 도달할 수 없는 경우 (는 없다고 주어지긴 했지만) None을 반환한다. 


&nbsp;  

## **다른 풀이**
---

```python  
import itertools


def is_movable(cur_x, cur_y, next_x, next_y, rectangles):
    x, y = (cur_x + next_x) / 2, (cur_y + next_y) / 2
    is_on_any_border = any(
        (x in (x1, x2) and y1 <= y <= y2) or (y in (y1, y2) and x1 <= x <= x2)
        for x1, y1, x2, y2 in rectangles)
    is_inside_any_rect = any(
        x1 < x < x2 and y1 < y < y2 for x1, y1, x2, y2 in rectangles)
    return is_on_any_border and not is_inside_any_rect


def solution(rectangle, characterX, characterY, itemX, itemY):
    cur_pos = (characterX, characterY)
    prev_pos = None
    for dist in itertools.count():
        if cur_pos == (characterX, characterY) and prev_pos:
            perimeter = dist
            break
        elif cur_pos == (itemX, itemY):
            dist_to_item = dist
        for dx, dy in ((1, 0), (-1, 0), (0, 1), (0, -1)):
            next_pos = (cur_pos[0] + dx, cur_pos[1] + dy)
            if next_pos != prev_pos and is_movable(*cur_pos, *next_pos,
                                                   rectangle):
                prev_pos, cur_pos = cur_pos, next_pos
                break
    return min(dist_to_item, perimeter - dist_to_item)
```

&nbsp;  


#### **코드 해설**  
1. 현재 칸에서 다음 칸으로 이동할 때 2로 나눈 중간점이 테두리에 있는지, 직사각형 내부에 있는지 여부를 반환하는 함수를 정의한다. 
2. itertools의 count를 사용해 거리 변수인 `dist`를 1씩 증가시키면서 조건을 판단한다. 
3. 시작지점으로 되돌아 왔을 때, `perimeter` 변수에 거리를 저장해 둘레를 계산한다. 
4. 현재 지점이 목표 지점과 같은 경우 현재까지 저장된 거리를 `dist_to_item` 변수에 할당한다.
5. 각 방향을 순회하며 다음 위치를 계산해줄 때 다음 위치가 유효하면 이전 위치와 현재 위치를 갱신한 뒤 반복문을 탈출한다. 
6. 계산된 거리와 총 둘레에서 계산된 거리를 뺀 값의 최솟값인 최단 경로를 반환한다. 


&nbsp;   
&nbsp;  
