---
title: "[프로그래머스] Lv.2 타겟 넘버 - Python"
date: 2025-01-10  
categories: [코딩테스트 연습, 프로그래머스]
tags: [Python, Algorithm]
layout: post
author: mongblang
---

# 📌&nbsp; **타겟 넘버**
[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/43165)  

# 📝&nbsp; **문제**
---
![문제](/assets/img/codingtest-post-img/PG43165-1.png)

## ✏️&nbsp; **입출력 예시**
---
![예시](/assets/img/codingtest-post-img/PG43165-2.png) 

&nbsp;  

&nbsp;   



## **내 풀이**  
--- 

```python
def solution(n, wires):
    def dfs(idx, sum):
        if idx == len(numbers):
            if sum == target:
                return 1
            else:
                return 0

        cnt_add = dfs(idx + 1, sum + numbers[idx])
        cnt_sub = dfs(idx + 1, sum - numbers[idx])

        return cnt_add + cnt_sub

    return dfs(0,0)
```


#### **코드 해설**  
1. 현재 인덱스와 합계를 매개변수로 각 숫자들을 더하고 빼줄 재귀함수를 설계한다.
2. 종료조건으로 numbers리스트에 모든 숫자들을 다 사용했을때 합계가 target과 같다면 1을, 아니면 0을 반환한다.
3. 재귀호출로 각 리스트의 모든 값을 더해 target이 나올 경우의 수를 반환할 변수 `cnt_add`를 정의한다.
4. 위와 같이 모든 값을 빼서 target이 나올 경우의 수를 반환할 변수 `cnt_sub`를 정의한다.
5. 모든 경우의 수를 더한 값을 반환 후 초기 호출로 인덱스 0, 합계0을 지정해 탐색의 결과값을 최종적으로 반환한다. 

&nbsp;  

## **다른 풀이**
---

```python  
def solution(numbers, target):
    if not numbers and target == 0 :
        return 1
    elif not numbers:
        return 0
    else:
        return solution(numbers[1:], target-numbers[0]) + solution(numbers[1:], target+numbers[0])
```

#### **코드 해설**  
1. numbers배열이 비어있거나 target이 0인 경우 가능한 방법이 1가지이므로 1을 반환한다. 
2. 배열이 비어있는데 target이 0이 아닌 경우 가능한 방법이 없으므로 0을 반환한다.
3. solution 함수를 재귀호출해서 첫 번째 숫자를 제외한 나머지와 타겟에서 첫 번째 숫자를 뺀 값을 통해 첫 번째 숫자를 더했으니, 남은 숫자들로 (target - 첫번째숫자)를 만든다. 
4. 다시 한번 재귀호출한 뒤 첫 번째 숫자를 빼고 남은 숫자들로 (target + 첫번째숫자)를 만든다.
5. 두 함수의 결과값을 더한 값을 반환한다.

이걸 역으로 풀 생각을 한게 대단... 


&nbsp;   
&nbsp;  
