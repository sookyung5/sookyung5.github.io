---
title: "[프로그래머스] Lv.3 여행경로 - Python"
date: 2025-01-15  
categories: [코딩테스트 연습, 프로그래머스]
tags: [Python, Algorithm]
layout: post
author: mongblang
---

# 📌&nbsp; **여행경로**
[문제 링크](https://school.programmers.co.kr/learn/courses/30/lessons/43164)  

# 📝&nbsp; **문제**
---
![문제](/assets/img/codingtest-post-img/PG43164-1.png)



## ✏️&nbsp; **입출력 예시**
---
![예시](/assets/img/codingtest-post-img/PG43164-2.png)

&nbsp;  

&nbsp;   



## **내 풀이**  
--- 

```python
from collections import defaultdict

def solution(tickets):
    adj_list = defaultdict(list)
    start = "ICN"
    
    for a, b in tickets:
        adj_list[a].append(b)
        adj_list[a].sort()
    
    def dfs(city):
        while adj_list[city]:
            next_city = adj_list[city].pop(0)
            dfs(next_city)
        route.append(city)
    
    route = []
    dfs(start)
    return route[::-1]
```

#### **코드 해설**  
1. 인접리스트를 생성해 각 도시와 인접한 도시들을 리스트에 추가한다. 
2. 연결된 도시들을 알파벳순으로 정렬한다.
3. 이동가능한 도시가 있을 때 까지, 즉 현재 도시에서 갈 수 있는 다른 도시가 있으면 맨 앞 도시를 꺼내 다음 이동할 도시로 설정한다. 
4. 재귀 호출해서 갈 수 있는 도시가 없을 때까지 반복한다. 
5. 더이상 이동할 수 없다면 현재 도시를 `route` 리스트에 추가한다. 
6. 경로를 저장할 `route` 리스트를 생성해준 뒤 탐색을 시작한다. 
7. 마지막으로 방문한 도시가 먼저 추가되기 때문에 `route`를 역순으로 뒤집어서 반환한다. 


&nbsp;  

## **다른 풀이**
---

```python  
from collections import defaultdict 

def dfs(graph, N, key, footprint):
    print(footprint)

    if len(footprint) == N + 1:
        return footprint

    for idx, country in enumerate(graph[key]):
        graph[key].pop(idx)

        tmp = footprint[:]
        tmp.append(country)

        ret = dfs(graph, N, country, tmp)

        graph[key].insert(idx, country)

        if ret:
            return ret


def solution(tickets):
    answer = []

    graph = defaultdict(list)

    N = len(tickets)
    for ticket in tickets:
        graph[ticket[0]].append(ticket[1])
        graph[ticket[0]].sort()

    answer = dfs(graph, N, "ICN", ["ICN"])

    return answer
```

&nbsp;  


#### **코드 해설**  
1. 


&nbsp;   
&nbsp;  
